= simple_form_for([@brewery, @batch]) do |f|
  = f.error_notification

  .form-inputs

    = render "batches/sections/projections", :f => f

    = render "batches/sections/brewing_day", :f => f

    = render "batches/sections/bottling_day", :f => f

    = render "batches/sections/conditioning_day", :f => f

  .form-actions
    = f.button :submit, :class => "btn btn-primary"
